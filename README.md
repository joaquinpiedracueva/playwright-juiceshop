# 🎭 Playwright - 🧃 Juice Shop

Requires [Node.js](https://nodejs.org/) and [Docker](https://www.docker.com/) (running).

```bash
git clone https://github.com/joaquinpiedracueva/playwright-automation-framework.git
cd playwright-automation-framework
npm install
npx playwright install --with-deps
npx playwright test
```
